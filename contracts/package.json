{
  "name": "@carry-vault/contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "test:gas": "forge test --gas-report",
    "coverage": "forge coverage --report lcov",
    "lint": "solhint 'src/**/*.sol' 'test/**/*.sol'",
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "slither": "slither ."
  },
  "devDependencies": {
    "solhint": "^5.0.0"
  }
}